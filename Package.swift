// swift-tools-version:4.0
import PackageDescription

let package = Package(
    name: "Swiftline",
    products: [
        .library(
            name: "Swiftline",
            targets: ["Swiftline"])
    ],
    targets: [
        .target(
            name: "Swiftline",
            dependencies: []),
    ]
)
